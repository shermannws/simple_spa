#include "AssignPatternStrategy.h"

Result AssignPatternStrategy::evaluateClause(Query& query, std::shared_ptr<PkbReader> pkbReader) const {
    // call pkbReader getter funcs
    // build result
    Result res;
    return res;
}