#include "AssignPatternStrategy.h"

void AssignPatternStrategy::evaluateClause(Query& query, Result& result, std::shared_ptr<PkbReader> pkbReader) const {
    // call pkbReader getter funcs
    // do sth to result
}