procedure proc {
    x = 1;
    a = b + c;
    k = i / (j + l);

    while (while + w < 1 % (2 + ww * www)) {
        assign = a + aa + aaa;
    }

    while (while != 2) {
        a = first - second;
    }

    while (while > 2) {
        A = 1 + 2 - 3 * 4 / 5 % 6;
    }

    if (if + i > ii / iii) then {
        a = first - second;
    } else {
        first = a - second;
    }

    if (if == 1) then {
        a = second - second;
    } else {
        k = i / i;
    }

    if (if <= else) then {
        k = i / (j + l);
    } else {
        l = i / (j + l);
    }
}

